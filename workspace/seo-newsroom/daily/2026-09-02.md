# Scout report — 2026-09-02

## Decision

No new URL, no catalog/curriculum/product-code change today. One carried-forward research item read via primary source and advanced to catalog-ready. Nothing worth publishing live today.

## Sources checked

- Live homepage, `/curriculum`, `sitemap.xml`, `robots.txt`, `llms.txt` — all fetched, all 200, all consistent with what shipped this week (124 guides everywhere, structured data present on home and curriculum, OG image tags resolving to the correct per-page images).
- `npm run validate:catalog` — 124 guides, 8 modules, 6 rungs, 60 guided hours, valid.
- `git log` / `git status` — reviewed the last 15 commits; confirmed the SEO technical foundation (analytics, llms.txt, OG images, JSON-LD, internal linking) and four external awesome-list PRs shipped this week are real and match repo state. Working tree clean except one pre-existing untracked file (`daily/2026-08-28.md`, predates this scout's scope).
- `workspace/seo-newsroom/queue.json`, `START-HERE.md` — read for current state before acting.
- Primary source read in full: https://www.anthropic.com/research/trustworthy-agents (Anthropic).
- Live web search for anything new since 2026-09-01 from OpenAI/Anthropic/Google DeepMind on agents/evals/safety — nothing surfaced that clears the admission gate beyond what's already tracked in the queue; search results were mostly secondary coverage (crypto-news aggregators, Wikipedia event pages) rather than primary sources.

## Catalog and curriculum maintenance findings

Nothing new. Technical foundation shipped this week (Vercel Analytics, `llms.txt`, per-page OG images, JSON-LD on home/curriculum, prerequisite-to-module internal linking, README search-phrasing paragraph) is verified live and unchanged since the last check. No regressions found.

## Scored opportunities (1)

**Trustworthy agents in practice** (Anthropic) — score 67 → catalog/existing-page-update tier.

- Real mechanism: a four-layer governance frame (model, harness, tools, environment) arguing agent oversight must span all four, not just model training.
- Supported by real findings: Claude checks in roughly twice as often on complex tasks vs. simple ones at similar interruption rates; Plan Mode (review the full action sequence before execution) addresses approval fatigue while preserving oversight.
- Weaker parts, and the reason this scored 67 rather than higher: the piece itself labels several claims (MCP benefits, subagent coordination patterns) as "exploring" with no results presented. Mixed evidence quality, not uniformly strong — this is a real deduction, not a formality.
- Distinct from what's already required: doesn't overlap cleanly with the existing OpenAI governance paper (operator/user model) or Running Codex Safely (harness-level practices) already required in Safety & Governance — it's a genuinely different organizing frame, not a restatement.
- Recommendation: catalog-only addition (optional tier, Safety & Governance or Agents & Tools), not required reading, in a future production session. Full detail in `queue.json`.

## Rejections

None outright rejected this cycle. No candidate scored below 40.

## Existing-page overlap

Trustworthy agents in practice overlaps `/curriculum/agents` and `/curriculum/safety-governance` but does not duplicate either module's current required or optional reading (see distinct-frame note above).

## Inputs needed from Tushar

1. OK to add "Trustworthy agents in practice" to the catalog (optional tier) in a future production session? (Not urgent — no other blocking input needed this cycle.)
2. None on distribution — the four open/closed awesome-list PRs and the two pending X-post drafts from prior cycles are outside this scout's scope; status is tracked separately in `workspace/seo-newsroom/distribution/`.
3. None on technical maintenance — nothing found broken.

## Recommendation

Zero new URLs. One catalog-only addition ready for a future production session (see above).

## Confidence and unknowns

High confidence on the technical-state findings (directly verified via live fetch and `validate:catalog`). Medium confidence on "Trustworthy agents in practice" scoring — the mixed evidence quality is a judgment call, not a hard fact; a stricter reviewer could reasonably score it a few points lower into watchlist territory. Two queue items (`openai-in-house-data-agent`, `openai-third-party-cyber-eval-incidents`) remain carried forward unread this cycle, consistent with the five-opportunity cap and this scout choosing depth over breadth on one item rather than shallow-restating all three.

## Next manual trigger

> Read `SEO-NEWSROOM-MANUAL.md` and the complete `workspace/seo-newsroom` state, then run the Zero to AI Native newsroom in scout mode end to end. Do not publish or deploy.
